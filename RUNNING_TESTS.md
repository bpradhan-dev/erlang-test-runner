# Running Tests Locally

To run the tests for the Erlang test runner locally, you need to have [Erlang](https://www.erlang.org/) and [rebar3](https://www.rebar3.org/) installed on your system.

Once installed, open your terminal, navigate to the root directory of this repository, and run the following command:

```bash
rebar3 ct
